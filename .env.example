# Optional override. The production URL already defaults to this address.
SITE_URL=https://dookdookgoose.superfun.games/

# Optional local audio authoring only. Never prefix this with VITE_.
ELEVENLABS_API_KEY=
